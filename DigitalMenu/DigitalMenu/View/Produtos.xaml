<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="DigitalMenu.View.Produtos">
    <ContentPage.Content>
        <StackLayout>
            <StackLayout Padding="40" BackgroundColor="LightGray">
                <Label x:Name="TituloCategoria" Text="" HorizontalOptions="CenterAndExpand" FontSize="40"/>
            </StackLayout>
            <StackLayout>
                <ListView x:Name="ListaProduto" HasUnevenRows="True">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <ViewCell>
                                <Grid Margin="5">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>
                                    <Label Text="Imagem" Grid.Column="0" Grid.RowSpan="2"/>
                                    <StackLayout Grid.Column="1" Grid.RowSpan="2">
                                        <Label Text="{Binding NomeProduto}" FontSize="24"/>
                                        <Label Text="{Binding Adicionais}" FontSize="24"/>
                                    </StackLayout>
                                    <Button Text="Adicionar" Grid.Column="2" Grid.Row="1" Clicked="DetalheProdutoAction" WidthRequest="15" HeightRequest="10"/>
                                </Grid>
                            </ViewCell>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>